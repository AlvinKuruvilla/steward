{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window. It calls the app's own `backend` command, which needs no grant here; the page gets nothing from the shell plugin.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
