/** Placeholder until the fold exists; the API it will read is not written. */
export function Component() {
  return (
    <p className="text-sm text-[var(--color-secondary)]">
      Nothing here yet. The inbox renders what the fold derives, and the fold is
      not written.
    </p>
  );
}
